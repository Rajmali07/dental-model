# Copy this file to .env and fill in real values.
# .env is for LOCAL DEVELOPMENT ONLY - it is gitignored and never read by the deployed
# Hugging Face Space. Space secrets are set separately under the Space's
# Settings -> Repository secrets (see SETUP.md).

# Hugging Face token with "write" access, used locally to push models/Space content.
HF_TOKEN=

# Root folder for datasets, if not using the default ./data
DATA_ROOT=./data

# Optional experiment tracking
WANDB_API_KEY=
WANDB_PROJECT=dental-model

# Optional: override default device for local dev (cpu / cuda / mps)
DEVICE=cpu
